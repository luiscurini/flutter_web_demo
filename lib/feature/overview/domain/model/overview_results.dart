class OverviewResults {
  String name;
  String? address;
  String photo;
  double latitude;
  double longitude;
  double rating;
  int price;
  String? tip;

  OverviewResults(this.name, this.address, this.photo, this.latitude,
      this.longitude, this.rating, this.price, this.tip);
}
